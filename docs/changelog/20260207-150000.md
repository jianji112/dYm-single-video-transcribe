# 批量编辑独立控制 + 自动同步设置

**日期**: 2026-02-07

## 待办

- [x] 为批量编辑中的每个设置项添加独立的启用复选框
- [x] 添加自动同步和 Cron 表达式设置到批量编辑
- [x] 更新 `batchUpdateUserSettings` 数据库函数支持 auto_sync/sync_cron
- [x] 更新 preload 层类型定义
- [x] 批量修改自动同步后触发调度器更新

## 改动文件

- `src/renderer/src/pages/settings/UsersPage.tsx` — 批量编辑 UI 和逻辑
- `src/main/database/index.ts` — `batchUpdateUserSettings` 增加 auto_sync/sync_cron 支持
- `src/preload/index.ts` — 更新 batchUpdateSettings 参数类型

## 结果

批量编辑对话框中每个设置项前都有复选框，用户可选择性地只修改需要的字段，未勾选的字段保持原值不变。新增自动同步开关和 Cron 表达式配置。
