# 用户管理分页与全选修复

## 待办
- [x] 修复全选逻辑：从选中全部过滤数据改为只选当前页数据
- [x] 修复表头 Checkbox 状态判断：改为基于当前页数据
- [x] 每页条数改为可输入任意正整数

## 结果
- `handleSelectAll` 改为只操作 `paginatedUsers`，支持跨页累积选择
- 表头 Checkbox `checked` 状态从 `filteredUsers` 改为 `paginatedUsers`
- 每页条数从固定下拉选项（10/20/50/100）改为数字输入框，支持任意正整数
